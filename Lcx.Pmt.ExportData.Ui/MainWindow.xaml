<Window
    x:Class="Lcx.Pmt.ExportData.Ui.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:Lcx.Pmt.ExportData.Ui"
    xmlns:vm="clr-namespace:Lcx.Pmt.ExportData.Ui.ViewModels"
    mc:Ignorable="d"
    Title="MainWindow"
    Height="450"
    Width="800"
    FontSize="16"
    d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}">

    <StackPanel Orientation="Vertical">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding GamePath}" />
            <Button
                Grid.Column="1"
                Content="选择游戏目录"
                Command="{Binding SelectGamePathCommand}"
                Padding="4" />
        </Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding ModsPath}" />
            <Button
                Grid.Column="1"
                Content="选择MOD工作目录"
                Command="{Binding SelectModsPathCommand}"
                Padding="4" />
        </Grid>
        <Button
            Content="开始"
            Command="{Binding StartCommand}"
            Padding="4" />
    </StackPanel>
</Window>
